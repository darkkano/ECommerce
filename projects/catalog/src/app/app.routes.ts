import { Routes } from '@angular/router';

// El MFE catalog también es una sola pantalla (preview en :4201).
// El shell no usa estas rutas: carga ProductGrid vía loadRemoteModule('catalog', './ProductGrid').
export const routes: Routes = [];
