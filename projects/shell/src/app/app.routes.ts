import { Routes } from '@angular/router';

// SPA de una sola vista: el shell no navega entre URLs.
// La “navegación” es el buscador semántico + el remote ProductGrid de Federation.
export const routes: Routes = [];
